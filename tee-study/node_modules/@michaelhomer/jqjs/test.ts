import jq, { JQValue } from './jq.js';

let foo = jq('.', 1);


let x = foo(1)

for (let y of foo) {}

let z : JQValue = true;

for (let y of x) {
    console.log(y);
    if (typeof y === 'number') {
        console.log('Number:', y);
    } else {
        y
    }
}